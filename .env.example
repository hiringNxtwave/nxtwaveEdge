# Frontend Fallback System Configuration
# Set to "true" to force using mock data even when API is available (useful for frontend development)
# In development mode, fallback will automatically activate when API calls fail
VITE_USE_MOCK_DATA=false

# Other environment variables...
# Add your other environment variables here